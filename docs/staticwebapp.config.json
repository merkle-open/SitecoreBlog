{
    "forwardingGateway": {
        "requiredHeaders": {
            "X-Azure-FDID": "${{ secrets.AZURE_FRONTDOOR_ID }}"
        },
        "allowedForwardedHosts": [
            "${{ secrets.BLOG_DOMAIN }}"
        ]
    }
}